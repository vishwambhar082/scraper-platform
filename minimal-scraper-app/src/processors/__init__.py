"""
Processors package.

Contains all data processing components including:
- Classic processors (regex, XPath, normalization)
- LLM processors (normalization, QC, PDF extraction)
- PDF processors (fetcher, text extractor, table extractor)
- Hybrid mode processors
"""

