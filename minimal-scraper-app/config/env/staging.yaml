# staging overrides
app:
  environment: staging

logging:
  level: INFO

database:
  host: staging-db.internal
  port: 5432
  user: scraper_staging
  password: staging_password
  name: scraper_db_staging

redis:
  enabled: true
  host: staging-redis.internal
  port: 6379
  db: 1
