# prod overrides
app:
  environment: prod

logging:
  level: INFO

database:
  host: prod-db.internal
  port: 5432
  user: scraper_prod
  password: prod_password_change_me
  name: scraper_db_prod

redis:
  enabled: true
  host: prod-redis.internal
  port: 6379
  db: 2
